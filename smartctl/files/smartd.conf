#
# DO NOT EDIT
#
# This file is managed by salt via {{ source }}
# Modify the config that generates this file instead
#

{% if smartctl.device_scan is defined and smartctl.device_scan -%}
DEVICESCAN -d removable -n standby -a -s (S/{{../.././01|L/../../6/03) -m {{ smartctl.email }} -M exec /usr/share/smartmontools/smartd-runner
{%- elif smartctl.devices is defined -%}
    {% for device in smartctl.devices -%}
    	/dev/{{ device }} -n standby -H -l error -l selftest -f -s (S/../.././04|L/../../6/05) -m {{ smartctl.email }} -M daily
    {% endfor -%}
{%- endif -%}
